 1. What is Semantic HTML? 
- It is HTML tags that clearly describes its purpose to the computer and to humans

    2. What is HTML used for? 
- HTML is used to build the 'skeleton' of the webpage

    3. What is an attribute and where do we put it? 
- An attribute is the place in which you want your anchor tags to go

    4. What is the h1 tag used for? How many times should I use i
    t on a page?
- The h1 would be the main header, ideally used once in a page
    5. Name two tags that have required attributes
- anchor tags and ids

    6. What do we put in the head of our HTML document? 
- The meta data

    7. What is an id? 
- An id specifies a unique id for an HTML element

    8. What elements can I add an id to? 
- You can add an id to a nav

    9. How many times can I use the same id on a page? 
- once

    10. What is a class? 
- A class is a reference you give to a tag to distiguish it from other tags

    11. What elements can I add a class to?
- For the most part any element, divs, sections, imgs, etc

    12. How many times can I use the same class on a page? 
- As many as you want

    13. How do I get my link to open in a new tab?
- target="_blank"

    14. What is the alt attribute in the image tag used for? 
- For screenread and accessability

    15. How do I reference an id?
- #idname

    16. What is the difference between a section and a div
- a section is semantic, a div is not

    17. What is CSS used for? 
- To style a webpage

    18. How to we select an element? Example - every h2 on the page
- h2 {
property:example;
}

    19. What is the difference between a class and an id? - Give me an example of when I might use each one
- Id is only used once for certain things such as classifying a nav. A class can be used more commonly and it is used to classify multiple sections of the page

    20. How do we select classes in CSS?
- .class {
property:example;
}

    21. How do we select a p element with a single class of “human””?
- p.human {
property:example;
}

    22. What is a parent child selector? When would this be useful? 
- It is when you target all the children of the parents by only targeting the parents, it would be useful when trying to target all children 

    23. How do you select all links within a div with the class of sidebar?
- div.sidebar {
property:example;
}

    24. What is a pseudo selector?
- It is when you are trying to select elements to do a specific task

    25. What do we use the change the spacing between lines?
- <br>

    26. What do we use to change the spacing between letters?
- div{
   letter-spacing: 2px; 
} 

    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize?
- div.a {
  text-transform: uppercase;
}

div.b {
  text-transform: lowercase;
}

    28. How do I add a 1px border around my div that is dotted and black?
- div {
border-style:dotted;
border-color:black;
}

    29. How do I select everything on the page? 
- * {
property:example;
}

    30. How do I write a comment in CSS?
- <!-- hello -->

    31. How do I find out what file I am in, when I am using the command line? 
- Use pwd

    32. Using the command line - how do I see a list of files/folders in my current folder?
- us ls

    33. How do I remove a file via the command line? Why do I have to be careful with this? 
- rm <file>

    34. Why should I use version control? 
- Version control is important so you can keep track of changes and go back to old code if needed.

    35. How often should I commit to github?
- Every 15-20 minutes/significant changes is good practice

    36. What is the command we would use to push our repo up to github? 
- The mac terminal or git bash

    37. Walk me through Lambda's git flow. 
- Fork the repo
- copy code 
- open CLI and cd into desired folder
- git clone 'link'
- cd into file
- change branch/ git checkout - b 'first-last'
- start coding
- save
- git add .
- git commit -m "example"
- git push -u origin first-last
- change base repo in github
- pull request


Stretch Questions

    1. What is the difference between an inline element and a block element?
  - inline: the block doesn't start in a new line and only uses the width required
  - block: will start a new line and on a new line and use the full width available.

    2. What happens when an element is positioned absolutely? 
  - The element is absolute meaning it over rides any other rule of position, it will not move from that place

    3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width? the "fix" for the box model, in other words, how do we make all elements respect the width we've given them? 
  - Use inline block


codepen
https://codepen.io/juancaruizc/pen/GRZvWem?editors=1100
https://codepen.io/juancaruizc/pen/dyMzJbL?editors=1100